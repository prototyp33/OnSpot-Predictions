-- SQL Script to create recommended indexes
-- Generated by analyze_query_patterns.py
-- Date: 2025-03-20 11:34:13

-- Indexes for table: business_metrics
-- Time-series data frequently queried by date range
CREATE INDEX IF NOT EXISTS idx_business_metrics_timestamp ON business_metrics USING btree (timestamp);

-- Indexes for table: location_metrics
-- Time-series data frequently queried by date range
CREATE INDEX IF NOT EXISTS idx_location_metrics_date ON location_metrics USING btree (date);

-- Indexes for table: system_health
-- Time-series data frequently queried by date range
CREATE INDEX IF NOT EXISTS idx_system_health_timestamp ON system_health USING btree (timestamp);

-- Indexes for table: ab_tests
-- Frequently used in queries
CREATE INDEX IF NOT EXISTS idx_ab_tests_id ON ab_tests USING btree (id);

-- Indexes for table: test_variants
-- Frequently used in queries
CREATE INDEX IF NOT EXISTS idx_test_variants_id ON test_variants USING btree (id);

-- Indexes for table: test_daily_metrics
-- Frequently used in queries
CREATE INDEX IF NOT EXISTS idx_test_daily_metrics_id ON test_daily_metrics USING btree (id);

-- Time-series data frequently queried by date range
CREATE INDEX IF NOT EXISTS idx_test_daily_metrics_date ON test_daily_metrics USING btree (date);

-- Indexes for table: users
-- Frequently used in queries
CREATE INDEX IF NOT EXISTS idx_users_id ON users USING btree (id);

-- Indexes for table: user_roles
-- Frequently used in queries
CREATE INDEX IF NOT EXISTS idx_user_roles_id ON user_roles USING btree (id);

-- Indexes for table: drift_analysis
-- Time-series data frequently queried by date range
CREATE INDEX IF NOT EXISTS idx_drift_analysis_timestamp ON drift_analysis USING btree (timestamp);

-- Time-series data frequently queried by date range
CREATE INDEX IF NOT EXISTS idx_drift_analysis_baseline_timestamp ON drift_analysis USING btree (baseline_timestamp);

-- Indexes for table: retraining_events
-- Time-series data frequently queried by date range
CREATE INDEX IF NOT EXISTS idx_retraining_events_timestamp ON retraining_events USING btree (timestamp);
